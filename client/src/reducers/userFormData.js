const defaultState = {
	username: '',
	email: '',
	password: '',
	password_confirmation: ''
}

export const UserFormData = (state=[], action) => {
	switch(action.type) {
		default:
			return state
	}
}